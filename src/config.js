/**
 * Type helper for VitePress themeConfig.
 * Returns the config object unchanged — exists only for TypeScript autocompletion.
 *
 * @param {import('./config').ThemeConfig} config
 * @returns {import('./config').ThemeConfig}
 */
export function defineThemeConfig(config) {
  return config
}
